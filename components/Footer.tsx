import styled from "styled-components";

const StyledFooter = styled.footer`
  width: 100vw;
  height: 15vh;
  background: purple;
`;

const Footer = () => {
  return <StyledFooter />;
};

export default Footer;
