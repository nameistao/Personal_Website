import styled from "styled-components";

const StyledHeader = styled.header`
  width: 100vw;
  height: 15vh;
  background: blue;
`;

const Header = () => {
  return <StyledHeader />;
};

export default Header;
